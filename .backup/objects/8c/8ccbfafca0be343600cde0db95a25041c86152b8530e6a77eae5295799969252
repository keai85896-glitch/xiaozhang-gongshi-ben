from pathlib import Path
p=Path('/data/user/0/com.ai.assistance.operit/files/workspace/b3f45e12-c3aa-438a-940d-2956d9b3337e/app/src/main/java/com/java/myapplication/MainActivity.kt')
s=p.read_text()
# Home card history entry
s=s.replace('FilledTonalButton(onClick = { onAction("salarySettings") }) { Icon(Icons.Default.Settings, null); Spacer(Modifier.width(4.dp)); Text("工资规则") }', 'Row(horizontalArrangement = Arrangement.spacedBy(6.dp), verticalAlignment = Alignment.CenterVertically) { TextButton(onClick = { onAction("salaryHistory") }) { Text("历史") }; FilledTonalButton(onClick = { onAction("salarySettings") }) { Icon(Icons.Default.Settings, null); Spacer(Modifier.width(4.dp)); Text("工资规则") } }')
# dialog handling
s=s.replace('''        "salarySettings" -> SalarySettingsDialog(settings = salarySettings, onDismiss = { dialog = null }, onSave = { store.saveSalarySettings(it); reload(); dialog = null; message = "工资设置已保存" })''', '''        "salarySettings" -> SalarySettingsDialog(settings = salarySettings, onDismiss = { dialog = null }, onSave = { store.saveSalarySettings(it); reload(); dialog = null; message = "工资设置已保存" })\n        "salaryHistory" -> SalaryHistoryDialog(records = work, settings = salarySettings, adjustments = salaryAdjustments, onDismiss = { dialog = null })''')
# WorkScreen state and overview signature
s=s.replace('''    var filter by remember { mutableStateOf(WorkFilter.Month) }\n    var calendarMonth by remember { mutableStateOf(monthText()) }\n    val today = dayText()''', '''    var filter by remember { mutableStateOf(WorkFilter.Month) }\n    var calendarMonth by remember { mutableStateOf(monthText()) }\n    var selectedDate by remember { mutableStateOf(dayText()) }\n    val today = dayText()''')
s=s.replace('''    val todayRecords = remember(records, today) { records.filter { it.date == today } }''', '''    val todayRecords = remember(records, today) { records.filter { it.date == today } }\n    val selectedRecords = remember(records, selectedDate) { records.filter { it.date == selectedDate } }''')
s=s.replace('''        item(key = "salary-overview") { SalaryOverviewCard(summary, period, missed.size, todayRecords.sumOf { it.hours }, periodRecords.map { it.date }.distinct().size) }''', '''        item(key = "salary-overview") { SalaryOverviewCard(summary, period, calendarMonth, missed.size, todayRecords.sumOf { it.hours }, periodRecords.map { it.date }.distinct().size, onBackToMonth = { calendarMonth = currentMonth; selectedDate = today }) }''')
s=s.replace('''        item(key = "calendar") { WorkCalendarCard(records = records, holidays = holidays, month = calendarMonth, onMonthChange = { calendarMonth = it }, onDateClick = { date -> records.filter { it.date == date }.maxByOrNull { it.id }?.let(onEdit) ?: onAddForDate(date) }) }''', '''        item(key = "calendar") { WorkCalendarCard(records = records, holidays = holidays, month = calendarMonth, selectedDate = selectedDate, onMonthChange = { calendarMonth = it; selectedDate = monthDays(it).firstOrNull() ?: it }, onDateClick = { date -> selectedDate = date }) }\n        item(key = "date-detail-$selectedDate") { WorkDateDetailCard(date = selectedDate, records = selectedRecords, holidays = holidays, isMissed = selectedDate in missed, onEdit = onEdit, onDelete = onDelete, onAddForDate = onAddForDate) }''')
s=s.replace('''        if (shown.isEmpty()) item(key = "empty-work") { EmptyCard("没有匹配的工时记录", "点右下角新增，或点击日历补录") }''', '''        if (shown.isEmpty()) item(key = "empty-work") { EmptyCard("没有匹配的工时记录", "点右下角新增，或在日期详情里补录") }''')
# replace SalaryOverviewCard function
old='''@Composable fun SalaryOverviewCard(summary: SalarySummary, period: SalaryPeriod, missedCount: Int, todayHours: Double, workDays: Int) { Card(Modifier.fillMaxWidth()) { Column(Modifier.padding(16.dp), verticalArrangement = Arrangement.spacedBy(12.dp)) { Column { Text("工时周期概览", style = MaterialTheme.typography.titleMedium, fontWeight = FontWeight.Bold); Text("${displayDate(period.start)} 至 ${displayDate(period.end)} · 已记录 $workDays 天", style = MaterialTheme.typography.labelMedium, color = MaterialTheme.colorScheme.onSurfaceVariant) }; Text(yuan(summary.netSalary), style = MaterialTheme.typography.headlineSmall, fontWeight = FontWeight.Bold, color = MaterialTheme.colorScheme.primary); Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.spacedBy(10.dp)) { AccountStatPill("实发工资", yuan(summary.netSalary), MaterialTheme.colorScheme.primaryContainer, Modifier.weight(1f)); AccountStatPill("总工时", "${fmt(summary.totalHours)}h", MaterialTheme.colorScheme.secondaryContainer, Modifier.weight(1f)); AccountStatPill("加班工资", yuan(summary.overtimeWage), MaterialTheme.colorScheme.tertiaryContainer, Modifier.weight(1f)) }; Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.spacedBy(10.dp)) { AccountStatPill("今日", "${fmt(todayHours)}h", MaterialTheme.colorScheme.surfaceVariant, Modifier.weight(1f)); AccountStatPill("漏记", "${missedCount}天", if (missedCount > 0) MaterialTheme.colorScheme.errorContainer else MaterialTheme.colorScheme.surfaceVariant, Modifier.weight(1f)); AccountStatPill("调休", "${fmt(summary.compLeaveHours)}h", MaterialTheme.colorScheme.surfaceVariant, Modifier.weight(1f)) } } } }'''
new='''@Composable fun SalaryOverviewCard(summary: SalarySummary, period: SalaryPeriod, calendarMonth: String, missedCount: Int, todayHours: Double, workDays: Int, onBackToMonth: () -> Unit) { Card(Modifier.fillMaxWidth()) { Column(Modifier.padding(16.dp), verticalArrangement = Arrangement.spacedBy(12.dp)) { Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.SpaceBetween, verticalAlignment = Alignment.Top) { Column(Modifier.weight(1f)) { Text("工时周期概览", style = MaterialTheme.typography.titleMedium, fontWeight = FontWeight.Bold); Text("当前查看：${monthTitle(calendarMonth)}", style = MaterialTheme.typography.labelMedium, color = MaterialTheme.colorScheme.onSurfaceVariant); Text("工资周期：${period.start} 至 ${period.end} · 已记录 $workDays 天", style = MaterialTheme.typography.labelMedium, color = MaterialTheme.colorScheme.onSurfaceVariant) }; TextButton(onClick = onBackToMonth) { Text("回本月") } }; Text(yuan(summary.netSalary), style = MaterialTheme.typography.headlineSmall, fontWeight = FontWeight.Bold, color = MaterialTheme.colorScheme.primary); Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.spacedBy(10.dp)) { AccountStatPill("实发工资", yuan(summary.netSalary), MaterialTheme.colorScheme.primaryContainer, Modifier.weight(1f)); AccountStatPill("总工时", "${fmt(summary.totalHours)}h", MaterialTheme.colorScheme.secondaryContainer, Modifier.weight(1f)); AccountStatPill("加班工资", yuan(summary.overtimeWage), MaterialTheme.colorScheme.tertiaryContainer, Modifier.weight(1f)) }; Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.spacedBy(10.dp)) { AccountStatPill("今日", "${fmt(todayHours)}h", MaterialTheme.colorScheme.surfaceVariant, Modifier.weight(1f)); AccountStatPill("漏记", "${missedCount}天", if (missedCount > 0) MaterialTheme.colorScheme.errorContainer else MaterialTheme.colorScheme.surfaceVariant, Modifier.weight(1f)); AccountStatPill("调休", "${fmt(summary.compLeaveHours)}h", MaterialTheme.colorScheme.surfaceVariant, Modifier.weight(1f)) } } } }'''
s=s.replace(old,new)
# WorkCalendar signature and selected pass
s=s.replace('@Composable fun WorkCalendarCard(records: List<WorkRecord>, holidays: List<HolidayRecord>, month: String, onMonthChange: (String) -> Unit, onDateClick: (String) -> Unit) {', '@Composable fun WorkCalendarCard(records: List<WorkRecord>, holidays: List<HolidayRecord>, month: String, selectedDate: String, onMonthChange: (String) -> Unit, onDateClick: (String) -> Unit) {')
s=s.replace('isToday = d == today, isMissed = d in missedDates, isRestDay = isSunday(d), onClick = { onDateClick(d) }, modifier = Modifier.weight(1f))', 'isToday = d == today, isSelected = d == selectedDate, isMissed = d in missedDates, isRestDay = isSunday(d), onClick = { onDateClick(d) }, modifier = Modifier.weight(1f))')
s=s.replace('@Composable fun CalendarDayCell(date: String, lunarText: String, hours: Double, multiplier: Double, isHoliday: Boolean, isToday: Boolean, isMissed: Boolean, isRestDay: Boolean, onClick: () -> Unit, modifier: Modifier = Modifier)', '@Composable fun CalendarDayCell(date: String, lunarText: String, hours: Double, multiplier: Double, isHoliday: Boolean, isToday: Boolean, isSelected: Boolean, isMissed: Boolean, isRestDay: Boolean, onClick: () -> Unit, modifier: Modifier = Modifier)')
s=s.replace('val border = if (isToday) BorderStroke(1.5.dp, MaterialTheme.colorScheme.tertiary) else null;', 'val border = when { isSelected -> BorderStroke(2.dp, MaterialTheme.colorScheme.primary); isToday -> BorderStroke(1.5.dp, MaterialTheme.colorScheme.tertiary); else -> null };')
s=s.replace('if (hasRecord || isToday || isMissed || isHoliday)', 'if (hasRecord || isToday || isSelected || isMissed || isHoliday)')
# insert new composables before AccountScreen
insert='''\n@Composable fun SalaryHistoryDialog(records: List<WorkRecord>, settings: SalarySettings, adjustments: List<SalaryAdjustment>, onDismiss: () -> Unit) {\n    val periods = remember(records, adjustments, settings) { salaryHistoryPeriods(records, adjustments, settings) }\n    var selected by remember(periods) { mutableStateOf<SalaryPeriod?>(null) }\n    AlertDialog(onDismissRequest = onDismiss, title = { Text(if (selected == null) "工资历史" else "工资历史详情") }, text = {\n        if (selected == null) {\n            LazyColumn(modifier = Modifier.fillMaxWidth().heightIn(max = 520.dp), verticalArrangement = Arrangement.spacedBy(8.dp)) {\n                items(periods, key = { it.label + it.start }) { p ->\n                    val s = calculateSalary(records, settings, adjustments, p); val days = records.filter { it.date in p.start..p.end }.map { it.date }.distinct().size\n                    OutlinedCard(Modifier.fillMaxWidth().clickable { selected = p }) { Column(Modifier.padding(12.dp), verticalArrangement = Arrangement.spacedBy(6.dp)) { Text("${p.start} 至 ${p.end}", fontWeight = FontWeight.Bold); Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.spacedBy(8.dp)) { AccountStatPill("实发", yuan(s.netSalary), modifier = Modifier.weight(1f)); AccountStatPill("工时", "${fmt(s.totalHours)}h", modifier = Modifier.weight(1f)) }; Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.spacedBy(8.dp)) { AccountStatPill("加班", yuan(s.overtimeWage), modifier = Modifier.weight(1f)); AccountStatPill("出勤", "${days}天", modifier = Modifier.weight(1f)) } } }\n                }\n            }\n        } else {\n            val p = selected!!; val s = calculateSalary(records, settings, adjustments, p)\n            LazyColumn(modifier = Modifier.fillMaxWidth().heightIn(max = 560.dp), verticalArrangement = Arrangement.spacedBy(8.dp)) {\n                item { Text("${p.start} 至 ${p.end}", color = MaterialTheme.colorScheme.onSurfaceVariant) }\n                item { SalaryDetailCard(s) }\n                item { OvertimeStatsCard(s) }\n                item { ShiftStatsCard(s) }\n            }\n        }\n    }, confirmButton = { if (selected == null) TextButton(onClick = onDismiss) { Text("关闭") } else TextButton(onClick = { selected = null }) { Text("返回列表") } }, dismissButton = { if (selected != null) TextButton(onClick = onDismiss) { Text("关闭") } })\n}\n\n@Composable fun WorkDateDetailCard(date: String, records: List<WorkRecord>, holidays: List<HolidayRecord>, isMissed: Boolean, onEdit: (WorkRecord) -> Unit, onDelete: (WorkRecord) -> Unit, onAddForDate: (String) -> Unit) {\n    val holiday = holidays.firstOrNull { it.enabled && it.date == date }\n    val total = records.sumOf { it.hours }; val normal = records.sumOf { it.effectiveNormalHours }; val overtime = records.sumOf { it.effectiveOvertimeHours }; val wage = records.sumOf { it.wage }\n    OutlinedCard(Modifier.fillMaxWidth()) { Column(Modifier.padding(16.dp), verticalArrangement = Arrangement.spacedBy(10.dp)) {\n        Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.SpaceBetween, verticalAlignment = Alignment.CenterVertically) { Column(Modifier.weight(1f)) { Text("当天详情", style = MaterialTheme.typography.titleMedium, fontWeight = FontWeight.Bold); Text("${displayDate(date)} · ${calendarSubText(date, holiday)}", color = MaterialTheme.colorScheme.onSurfaceVariant) }; FilledTonalButton(onClick = { onAddForDate(date) }) { Text("补录") } }\n        Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.spacedBy(8.dp)) { AccountStatPill("总工时", "${fmt(total)}h", modifier = Modifier.weight(1f)); AccountStatPill("正常", "${fmt(normal)}h", modifier = Modifier.weight(1f)); AccountStatPill("加班", "${fmt(overtime)}h", modifier = Modifier.weight(1f)) }\n        Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.spacedBy(8.dp)) { AccountStatPill("当天工资", yuan(wage), modifier = Modifier.weight(1f)); AccountStatPill("状态", when { isMissed -> "漏记"; holiday != null -> holiday.name.ifBlank { holiday.type }; isSunday(date) -> "休息日"; else -> "普通" }, if (isMissed) MaterialTheme.colorScheme.errorContainer else MaterialTheme.colorScheme.surfaceVariant, Modifier.weight(1f)) }\n        if (records.isEmpty()) Text("暂无工时记录，可点击“补录”记录这一天。", color = MaterialTheme.colorScheme.onSurfaceVariant) else records.forEach { r -> WorkRow(r, onEdit, onDelete) }\n    } }\n}\n'''
s=s.replace('\n@Composable fun AccountScreen(records: List<AccountRecord>, modifier: Modifier, onEdit: (AccountRecord) -> Unit, onDelete: (AccountRecord) -> Unit) {', insert+'\n@Composable fun AccountScreen(records: List<AccountRecord>, modifier: Modifier, onEdit: (AccountRecord) -> Unit, onDelete: (AccountRecord) -> Unit) {')
# add helper funcs before shiftMonth
helper='''\nprivate fun salaryHistoryPeriods(records: List<WorkRecord>, adjustments: List<SalaryAdjustment>, settings: SalarySettings): List<SalaryPeriod> {\n    val months = (records.map { it.date.take(7) } + adjustments.map { it.date.take(7) } + monthText()).filter { it.length == 7 }.toMutableSet()\n    var cursor = monthText()\n    repeat(12) { months.add(cursor); cursor = shiftMonth(cursor, -1) }\n    return months.sortedDescending().map { salaryPeriodOf("$it-15", settings) }.distinctBy { it.start + it.end }\n}\n'''
s=s.replace('\nprivate fun shiftMonth(month: String, offset: Int): String {', helper+'\nprivate fun shiftMonth(month: String, offset: Int): String {')
p.write_text(s)
print('patched phase1')
